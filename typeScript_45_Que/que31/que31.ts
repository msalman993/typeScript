// Que 31
//Add an if test to make sure the list of users is empty.

//• If the list is empty, print the message We need to find some users!

let userNames: string[] = [];
if (userNames.length === 0) {
    console.log("We need to find some users!")
}